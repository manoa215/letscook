# # two containers in one pod
# apiVersion: v1
# kind: Pod
# metadata:
#   name: double
#   labels:
#     app: double
# spec:
#   terminationGracePeriodSeconds: 0
#   restartPolicy: Never
#   containers:
#   - name: one
#     image: busybox #change to nginx:1.17-alpine
#   - name: two
#     image: busybox #change to nginx:1.17-alpine
---
# # two containers accessing same port
# apiVersion: v1
# kind: Pod
# metadata:
#   name: multi
#   labels:
#     app: multi
# spec:
#   terminationGracePeriodSeconds: 0
#   restartPolicy: Never
#   containers:
#   - name: one
#     image: nginx:1.16-alpine #change to nginx:1.16-alpine
#   - name: two
#     image: shadjachaudhari/nginx:8080 #change to shadjachaudhari/nginx:8080
#   - name: three
#     image: shadjachaudhari/nginx:8080
#     command: ["/bin/sh"]
#     args: ["-c","curl -I localhost && curl -I localhost:8080"] #add sleep to hold logs